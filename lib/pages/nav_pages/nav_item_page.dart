import 'package:flutter/material.dart';

class NavItemPage extends StatelessWidget {
  const NavItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Nav item page")),
    );
  }
}
